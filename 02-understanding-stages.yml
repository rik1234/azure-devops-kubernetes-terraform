trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  jobs:
    - job: FirstJob
      steps: 
      - bash: echo Build FirstJob
    - job: SecondJob
      steps:
      - bash: echo Build SecondJob
- stage: DevDeploy
  jobs:
    - job: DevDeployJob
      steps: 
      - bash: echo Build FirstJob
- stage: QADeploy
  jobs:
    - job: QAJob
      steps: 
      - bash: echo Build FirstJob
- stage: ProDeploy
  jobs:
    - job: ProJob
      steps: 
      - bash: echo Build FirstJob

